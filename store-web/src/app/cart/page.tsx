import CartView from '@/app/cart/view'

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Shopping Cart'
}

const CartPage = () => {
  return <CartView />
}

export default CartPage
